{
  "name": "deadshot-viewer",
  "version": "1.0.0",
  "description": "Electron app that displays deadshot.io",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist-win": "electron-packager . omniverse --platform=win32 --arch=x64 --out=dist --overwrite",
    "dist-linux": "electron-packager . omniverse --platform=linux --arch=x64 --out=dist --overwrite"
  },

  "devDependencies": {
    "electron": "^26.6.10",
    "electron-packager": "^17.1.1"
  },
  "dependencies": {
    "electron": "^26.6.10",
    "electron-packager": "^17.1.1",
    "@cliqz/adblocker-electron": "^1.34.0",
    "cross-fetch": "^4.1.0",
    "discord-rpc": "^4.0.1"
  }
}
